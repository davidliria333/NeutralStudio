# Evidence packet

## Current project

- React 18 + Vite 5 + React Router + Framer Motion: `package.json`, `src/App.jsx`.
- ScrollCraft is already loaded from `public/scrollcraft.js` and mounted by the home page. The engine remains unmodified.
- Brand assets and tokens exist in `public/brand-logo.svg`, `public/brand-favicon.svg` and `src/styles/global.css`.
- Real named project assets exist in the repository, but the user's later instruction explicitly forbids reusing projects. They are excluded from the redesign.
- The verified conversation destination is `https://cal.com/neutralstudio/30min?overlayCalendar=true`.
- Existing testimonials, dynamic availability and performance/result claims have no provenance in the repository. They will not be used as proof.
- The canonical URL in `index.html` is a placeholder and must be corrected.

## Reusable patterns

- The ScrollCraft runtime already provides stage progress, cue, reveal, pan, parallax and reduced-motion fallbacks.
- Existing semantic section markup and responsive media rules are worth keeping as engineering patterns, not as visual structure.
- The previous home signature and assembly are already fingerprinted and cannot carry the redesign.

## Reference conclusion

Kree8 is used only to calibrate pacing, typographic authority, section variety, controlled grid-breaking, transitions and proof placement. Neutral's distinctive system is registration marks, calibration frames, baselines and the Personality control, using only Neutral's copy and project assets.

## Constraints

- All visible copy is English.
- No invented clients, quotes, figures, awards or outcomes.
- The user explicitly supplied `UXUI/` as the portfolio source after excluding the named projects. Generated Neutral studies support the site identity without claiming clients or results.
- Mobile is recomposed, not simply stacked.
- Reduced motion preserves reachability and meaning.
- The previous fingerprint row must remain untouched; the new row is appended only after verification.
